# web
# 데이터를 구축
# Database, 파일로부터 데이터를 얻어올 수 있다.
# 프로그램적으로 web scraping, crawling을 통해 데이터를 구축

# HTML, CSS, JavaScript

# HTML : 웹페이지의 내용, 구조를 담당
# CSS : 웹페이지의 styling을 담당
# JavaScript : 웹페이지의 동적처리를 담당

# 기본적으로 web은 CS구조로 이루어져있다.
# CS구조 : client-server 구조
# server쪽 프로그램 : web server, HTML,CSS,JavaScript를  제공
# client쪽 프로그램 : chrome(browser)
# 서버로부터 HTML, CSS,JavaScript를 받아서 화면에 rendering을 한다.

# HTML, CSS, JavaScript를 이용해서 클라이언트에게 제공해주는 web page를 만들어보자
# 개발툴이 있어야한다.
# eclipse 가 가장 일반적이지만 webstorm을 이용해보자



# 클라이언트에게 제공할 HTML을 작성했다(소리없는 아우성)
# 서버프로그램(web server)에게 우리 프로젝트를 알려줘야한다!
# 우리 프로젝트를 web server에게 인지시켜야한다. (configure시킨다)
# web server program을 기동시켜서 우리 프로젝트를 웹에 deploy
# web client(browser)를 이용해서 URL(주소, 좌표)서버쪽 프로그램에 접속해서 HTML을 받아가서 화면에 그리자
# web server가 webstorm에 내장되어있다!




























